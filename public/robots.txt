# Allow all bots
User-agent: *

# Disallow crawling of API routes and private application pages
Disallow: /api/
Disallow: /app/
Disallow: /admin/

# Allow everything else (like your homepage, privacy policy, etc.)
Allow: /
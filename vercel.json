{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    }
  ],
  "crons": [
    {
      "path": "/api/daily-refresh",
      "schedule": "0 1 * * *"
    }
  ]
}

# Database (same Neon instance as Market Pulse)
DATABASE_URL=postgresql://...

# SendGrid (optional — for summary emails)
SENDGRID_API_KEY=

# Residential proxy (recommended for stability)
# If using https-proxy-agent, set this:
# HTTPS_PROXY=http://user:pass@proxy-host:port
